{
  "mcpServers": {
    "local": {
      "command": "python",
      "args": ["server/server.py", "--mode", "local"],
      "env": {
        "MCP_SERVER_MODE": "local"
      }
    },
    "remote": {
      "url": "https://mcp-server-630744526491.me-west1.run.app",
      "auth": {
        "username": "admin",
        "password": "securepassword"
      },
      "env": {
        "MCP_SERVER_MODE": "remote"
      }
    }
  }
}
